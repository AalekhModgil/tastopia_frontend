export interface RestaurantModel {
  id: number;
  name: string;
  address: string;
  city: string;
  state: string;
  contactNumber: string;
  cuisine: string;
  imageUrl: string;
  createdAt: string;
  updatedAt: string;
  open: boolean;
}